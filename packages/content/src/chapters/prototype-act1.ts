import { prototypeScenes } from "../../manifests/prototype-scenes";
import prototypeAct1Compiled from "../../compiled/prototype-act1.json";

/** Dev fixture payload (precompiled; not in production catalog). */
export const scenes = prototypeScenes;
export const compiled = prototypeAct1Compiled;
