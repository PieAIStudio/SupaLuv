# branch/

**Owns**: constrained AI side-branch types, prompts, Mastra generation, choice-stats store.

**Does not own**: HTTP route table, safety gate, wallet, AI final chapters, character packs.

**Entry**: `mastraBranch.ts` + `prompts.ts` (generation); `branchTypes.ts` (contracts); `choiceStatsStore.ts` (stats).
