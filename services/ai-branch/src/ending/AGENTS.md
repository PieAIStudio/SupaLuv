# ending/

**Owns**: AI final-chapter session orchestration, prompts, schemas, Mastra generator, ending HTTP routes.

**Does not own**: short AI side branches, character generation, wallet commit logic, TTS.

**Entry**: `endingRoutes.ts` (HTTP); `endingSessionService.ts` (session domain); `mastraEnding.ts` (generator).
